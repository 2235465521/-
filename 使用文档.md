# ZKBZ 标准 PDF 下载 — 使用文档

> 版本：3.0.0（Web 界面 v4.1）  
> 适用：日常检索、高级筛选、批量下载

---

## 1. 系统简介

**ZKBZ** 是一套本地/内网标准 PDF 检索与下载工具，通过浏览器访问。主要能力：

| 模块 | 说明 |
|------|------|
| **标准检索** | 按标准号、名称检索；高级筛选含产品/品种扩展 |
| **批量下载** | 上传 Excel/CSV 清单，自动匹配并打包 ZIP |

数据来源：MySQL 标准库 + 磁盘 PDF 目录。

---

## 2. 启动与停止

### 2.1 启动（推荐）

双击以下任一文件（效果相同）：

- **`启动ZKBZ.bat`**（推荐）
- **`启动.bat`**
- **`start_zkbz.bat`**

启动成功后：

- 黑窗口保持打开（**关闭即停止服务**）
- 浏览器自动打开：**http://127.0.0.1:5000/**

### 2.2 首次运行（含从 GitHub 克隆）

**一键启动：** 双击 `启动ZKBZ.bat`。

脚本会自动：

1. 安装 Python 依赖（如缺失）  
2. 生成 `.env`（如缺失）  
3. 连接 MySQL；若标准库为空，从 `data/db_dump/*.sql.gz` **自动导入**  
4. 启动服务并打开浏览器  

前置条件：本机已安装并启动 **MySQL**（或先 `docker compose up -d mysql`，`.env` 中 `MYSQL_PASSWORD=zkbz`）。  
大备份若克隆后目录为空，请先 `git lfs pull`。

页眉应显示「数据库: MySQL · 就绪」。下载 PDF 还需配置 `PDF_ROOT`。

### 2.3 停止

直接关闭启动时的黑色命令行窗口即可。

---

## 3. 界面说明

### 3.1 左侧导航

- **标准检索**：主功能，编号/名称 + 高级筛选（含产品/品种）
- **批量下载**：Excel 清单批量打包

### 3.2 顶部搜索框

- 输入标准编号或名称关键词，点 **检索**

### 3.3 结果表格

- 点击行可展开，查看 PDF 文件列表
- 勾选左侧复选框 → 点 **多项下载** 打包当前选中项
- 结果上方蓝色条显示当前生效的高级筛选条件

---

## 4. 标准检索

### 4.1 基本检索

示例：

- 标准号：`GB/T 16472-2024`
- 名称关键词：`煤矿`、`健康信息学`

### 4.2 高级筛选

点 **高级选项** 展开面板，常用字段：

| 字段 | 说明 |
|------|------|
| 状态 | 现行 / 即将实施 / 废止 |
| 标准类型 | 国标、行标等 |
| 省 / 市 / 县 | 按**全部起草单位**通讯地址归类 |
| 产品/品种 | 可手输或下拉联想，命中词库会扩展关键词 |
| 公司/起草单位 | 输入 2 字以上自动联想，如输入「福建」列出相关单位 |
| 起草顺位 | 须配合「公司/起草单位」使用，见下表 |
| 年份起 / 年份止 | 可按发布年份范围筛选 |

**起草顺位选项：**

| 选项 | 含义 |
|------|------|
| 不限 | 该单位出现在任一顺位即可 |
| 第 1 / 2 / 3 起草单位 | 仅匹配该单位排在对应位次的标准 |
| 大于三 | 该单位排在第 4 位及以后 |

示例：查「福建理工大学」作为**第一起草单位**的标准：

1. 公司/起草单位：`福建理工大学`
2. 起草顺位：**第 1 起草单位**
3. 点 **应用筛选**

> 注意：顺位筛选必须同时填写单位名；只选顺位不填单位不会生效。

### 4.3 地区批量下载

1. 高级筛选中**必须先选择省份**
2. 可叠加市、县、产品、单位、年份等条件
3. 点 **地区批量下载** → 确认数量 → 打包 ZIP  
4. 单次最多 **5000** 条

---

## 5. 批量下载

1. 左侧切换到 **批量下载**
2. **下载模板** → 填写标准号（可选名称）
3. **上传** `.xlsx` / `.csv`（单次最多 **400** 条）
4. 预览匹配结果 → **打包下载 ZIP**

---

## 6. 配置说明

项目根目录 `.env` 可配置（无则启动时从 `.env.example` 自动生成）：

| 变量 | 说明 | 默认 |
|------|------|------|
| `PORT` | 服务端口 | `5000` |
| `OPEN_BROWSER` | 启动后是否打开浏览器 | `true` |
| `PDF_ROOT` | 标准 PDF 根目录 | 自动检测 E:/Z: 盘 |
| `MYSQL_HOST` / `MYSQL_PORT` / `MYSQL_USER` / `MYSQL_PASSWORD` / `MYSQL_DATABASE` | MySQL 连接 | 见 `.env.example` |
| `MYSQL_EXE` | mysql 客户端路径（可选） | 自动探测 |

标准库备份位于 `data/db_dump/`，由 `scripts/setup_mysql.py` 在首次启动时导入。  
也可用 `docker compose up -d mysql`（默认 root / `zkbz`）再启动本系统。

修改 `.env` 后需**重启**启动脚本。系统仅使用 MySQL。

---

## 7. 常见问题

### 启动后浏览器打不开

- 确认黑窗口无报错，访问 http://127.0.0.1:5000/
- 若端口被占用，启动脚本会先尝试结束旧进程

### 检索一直转圈 / 很慢

- 按 **Ctrl+F5** 强制刷新页面
- 重启 `启动ZKBZ.bat`
- 单位联想输入至少 **2 个字** 才会请求

### 选了「第 1 起草单位」仍出现非第一单位的标准

- 确认已填写 **公司/起草单位** 并点了 **应用筛选**
- 看结果上方蓝色筛选条是否含「仅第 1 位」
- 部分标准该单位排第一但**无 PDF 记录**，默认只显示有 PDF 的条目

### 按省/市/单位筛选无结果

- 确认 MySQL 中已有 `area_dict` / `unit_dict` / `std_unit_relation` 等表
- 确认 `.env` 中 MySQL 连接正确且服务已启动

### 页面样式或功能不对

- **Ctrl+F5** 强刷清除缓存
- 确认启动的是最新版 `启动ZKBZ.bat`

---

## 8. 目录结构（简要）

```
std_download/
├── 启动ZKBZ.bat          # 启动入口
├── start_zkbz.bat        # 实际启动逻辑
├── 使用文档.md           # 本文档
├── backend/              # Flask 后端
├── frontend/             # 网页界面
├── core/                 # 检索、筛选、下载逻辑
├── data/                 # 产品词库等
├── scripts/              # 备份导入等辅助脚本
├── .env                  # MySQL / PDF_ROOT 等配置
└── requirements.txt      # Python 依赖
```

---

## 9. 技术支持

遇到无法解决的问题，请保留：

1. 启动黑窗口的完整报错截图  
2. 浏览器 F12 控制台中的错误（如有）  
3. 操作步骤（检索词、筛选条件）

联系维护人员处理。
